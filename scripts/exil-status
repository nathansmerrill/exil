#!/bin/bash

cd $EXIL_DIR

if [ -s server/log/exil.pid ]; then
	if ps -p $(cat server/log/exil.pid) > /dev/null; then
		echo '[EXIL]' Server running with pid $(cat server/log/exil.pid)
	else
		echo '[EXIL]' Server crashed
	fi
else
	echo '[EXIL]' No server PID file found
fi
